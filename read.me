Tiled layer setup you'll need
In your .tmx file, create:

Tile layers: ground (index 0), decoration (index 1), above_player (index 2)
Object layer named walls — draw rectangles over every solid tile/area
Object layer named spawns — place point objects, set their type to spawn and name to e.g. player_start, enemy_1, etc.

